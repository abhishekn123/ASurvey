import { SurveyDateValidatorDirective } from './survey-date-validator.directive';

describe('SurveyDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new SurveyDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
